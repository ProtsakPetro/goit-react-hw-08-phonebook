import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#646666',
      main: '#818485',
      dark: '#5c5c5c',
      contrastText: '#fff',
    },
    secondary: {
      light: '#e0c075',
      main: '#f5de0c',
      dark: '#c9b904',
      contrastText: '#000',
    },
  },
});